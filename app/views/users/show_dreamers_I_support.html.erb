<h1>People Whose Dreams I've Invested In</h1>

<% if  @user.dreams_funded.where(funder_user_id: @current_user.id).each do |dr| %>
    <ol>
    <li><%= link_to "You funded #{dr.dreamer_user.email}'s dream of experiencing #{dr.name}.", dream_path(dr) %></a></li>
    </ol>
    <% end %>
<% else %>
    <p>No dreamers supported yet.</p>
<% end %>
