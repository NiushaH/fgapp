<!DOCTYPE html>
<html>
  <head>
    <title>fgapp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% if session[:current_user_id] %>
      <nav>        
        <section>
          <%= link_to_unless_current "Home", root_path %> |
          <%= link_to_unless_current "Dreams To Support", dreams_path %> |
          <%= link_to_unless_current "Request Funding for a Dream", new_dream_path %> |
          <%= link_to "My Dreams", users_dreams_path(:id) %> |
          <%= link_to "My Investments in Others", user_funds_path(:id) %>
        </section>
      
        <section class="utility">
          <% if session[:current_user_id] %>
          You are logged in as: <%= @current_user.email %> | 
          <a href="/logout">Logout</a>
          <% else %>
          <%= link_to_unless_current "Login", login_path %> |
          <%= link_to_unless_current "Register", register_path %> 
          <% end %>
        </section>

      </nav>
    <% end %>
    <%= yield %>
  </body>
</html>
